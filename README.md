# ব্লগ পোস্ট - ০১ ( টাইপস্ক্রিপ্টে ইন্টারফেস এবং টাইপের মধ্যে পার্থক্য )

ধরুন আপনার জীবন অনেক রকম সমস্যায় পরিপূর্ণ ছিল। আপনি কিছুতেই সেই সমস্যা কেন হচ্ছে, কী করলে এর সমাধান পাওয়া যাবে — কিছুই বুঝে উঠতে পারছেন না। ঠিক তখনই আপনার জীবনে মহান আল্লাহ এমন একজন মানুষকে পাঠালেন যিনি আপনার জীবনের সকল সমস্যা খুঁজে বের করে তা কীভাবে সমাধান করা যায় সব বলে দিতে লাগলেন। এ যেন কোনো মানুষ রূপী ফেরেশতা!

টাইপস্ক্রিপ্টও ঠিক আমাদের ডেভেলপারদের জন্য এমনি এক মানুষ — যা আমাদের কোডের সব বাগ একবারে ধরে ফেলতে সক্ষম। আমার এক টুইটার ফ্রেন্ড তার এক টুইটে বলেছিলো: “টাইপস্ক্রিপ্ট তোমার জীবনকে ভয়াবহ করে তুলছে না। এটা শুধু তোমাকে দেখাচ্ছে যে তোমার জীবন ইতিমধ্যেই কতটা ভয়াবহ...😅😅”

তাহলে চলুন আজকে আমরা টাইপস্ক্রিপ্টের একেবারে বেসিক দুইটি বিষয় — Type (type aliases) এবং Interface — সম্পর্কে জানার চেষ্টা করি।

---

## Interface vs Type — কোনটা কখন ব্যবহার করবেন?

TypeScript-এ type aliases এবং interface অনেকটাই একই রকম কাজ করে। বেশিরভাগ ক্ষেত্রে দুটোই interchangeably ব্যবহার করা যায়। তবে তাদের মধ্যে একটি গুরুত্বপূর্ণ পার্থক্য হলো:

> Type একবার ডিফাইন করলে পরে পরিবর্তন বা "reopen" করা যায় না, কিন্তু Interface যেকোনো সময় extend বা নতুন property যোগ করা যায়।

নিম্নে কয়েকটি উদাহরণ দিয়ে বোঝানো হলো।

### ১. নতুন ফিল্ড যোগ করা (Adding new fields)

✔ Interface-এ নতুন ফিল্ড যোগ করা যায়:

```ts
interface User {
  id: string;
  email: string;
}

interface User {
  name: string;
}

// এখন User interface এ তিনটি property থাকবে
const user: User = {
  id: "2323232",
  email: "foo@email.com",
  name: "Foo"
}
```

❌ Type-এ নতুন ফিল্ড যোগ করা যায় না:

```ts
type User = {
  id: string;
}

type User = {
  email: string;
}

// ❌ ERROR: Duplicate identifier "User"
```

সংক্ষেপে: Interface পরিবর্তনযোগ্য, কিন্তু Type নয়। Once a type is created, that's final.

### ২. Extends (Interface vs Type)

Interface এবং Type extend করার পদ্ধতি আলাদা।

✔ Interface extends (extends keyword)

```ts
interface Car {
  model: string;
  color: string;
}

interface Tesla extends Car {
  autoPilotModelName: string;
}

const newCar: Tesla = {
  model: "S",
  color: "red",
  autoPilotModelName: "xyz"
}
```

✔ Type extends (intersection &)

```ts
type Car = {
  model: string;
  color: string;
}

type Tesla = Car & {
  autoPilotModelName: string;
}

const newCar: Tesla = {
  model: "S",
  color: "red",
  autoPilotModelName: "xyz"
}
```

দুটোতেই extend করা যায়, শুধু syntax আলাদা।

### ৩. Union Types (Type-এ করা যায়, Interface-এ সরাসরি যায় না)

❌ Interface-এ union assign করা যায় না (এভাবে নয়):

```ts
interface User {
  email: string;
}

interface Admin {
  email: string;
  adminKey: string;
}

// ❌ ERROR: '{' expected.
interface Person = User | Admin;
```

✔ কিন্তু type দিয়ে সহজে করা যায়:

```ts
type Person = User | Admin;
```

✔ Interface-এ চাইলে union ভেতরের property হিসেবে ব্যবহার করা যায়:

```ts
interface Person {
  person: User | Admin;
}
```

Type উদাহরণ:

```ts
type User = {
  email: string;
}

type Admin = {
  email: string;
  adminKey: string;
}

type Person = User | Admin; // perfectly valid
```

কারণ interface ক্লাসের মতো আচরণ করে — `interface Something = ...` এই syntax টি TypeScript সাপোর্ট করে না।

---

## শেষ কথা

TypeScript-এ দুইটিই শক্তিশালী এবং পরিস্থিতি অনুযায়ী ব্যবহার করা উচিত। সংক্ষেপে নির্দেশিকা:

- Object structure define করতে চাইলে: **interface**
- Union, intersection বা complex type লাগলে: **type**
- একই interface-এ নতুন ফিল্ড পরে যোগ করতে চাইলে: **interface**
- Type একবার বানালে আর modify করা যাবে না: **type**

//*---------------------------*//


## ব্লগ পোস্ট - ০২ (TypeScript এ Union এবং Intersection Type সহজভাবে বোঝা)

TypeScript এ Union এবং Intersection টাইপ আমাদের কোডকে আরও টাইপ-সেফ এবং ফ্লেক্সিবল করে। নিচে সংক্ষেপে ব্যাখ্যা ও উদাহরণ দেওয়া হলো।

### Union Type
Union Type তখন ব্যবহার করা হয় যখন কোনো ভেরিয়েবল বা ফাংশন একাধিক টাইপ গ্রহণ করতে পারে — অর্থাৎ "OR"। পাইপ (|) সিম্বল দিয়ে Union বানানো হয়।

উদাহরণ:
```ts
const checkNum: number | string;
checkNum = 10;
checkNum = "Hello";
checkNum = true; // Error: কেবল number বা string গ্রহণ করা হবে
```

ফাংশনের প্যারামিটারেও ব্যবহার:
```ts
const sayHi = (param: string | number) => {
  console.log(param);
}

sayHi("Hello!");
sayHi(89.6);
sayHi(true); // Error
```

Literal Type-এর সাথে Union:
```ts
type Sports = 'cricket' | 'football' | 'tennis';

function play(game: Sports) {
  console.log(`You are playing ${game}.`);
}

play('cricket');
```

### Intersection Type
Intersection Type (&) ব্যবহার করে একাধিক টাইপকে একত্রে মিশিয়ে একটি পূর্ণাঙ্গ টাইপ তৈরি করা হয় — অর্থাৎ সব টাইপের প্রপার্টি একসাথে থাকবে।

উদাহরণ:
```ts
type A = { x: number };
type B = { y: string };

type C = A & B; // C কে দুইটি টাইপের প্রপার্টি থাকবে: x এবং y

const value: C = { x: 10, y: "hello" };
```

Interface একত্রিত করা:
```ts
interface Student {
  name: string;
  age: number;
}

interface Teacher {
  teachingId: string;
}

type Person = Student & Teacher;

const p: Person = {
  name: "Karim",
  age: 30,
  teachingId: "CSE123"
};
```

Intersection Type দিয়ে টাইপ এক্সটেন্ড করা:
```ts
type Area = { width: string; height: string };
type Volume = Area & { depth: string };
```

## শেষ কথা
- Union Type = OR (একাধিক সম্ভাব্য টাইপের মধ্যে একটি)  
- Intersection Type = AND (সব টাইপের প্রপার্টি মিলিয়ে একটি টাইপ)  

প্রয়োজনে Union ব্যবহার করুন যখন ভ্যারিয়েবল বিভিন্ন ধরন ধরতে পারে; আর Intersection ব্যবহার করুন যখন একসাথে সব প্রপার্টি প্রয়োজন।
